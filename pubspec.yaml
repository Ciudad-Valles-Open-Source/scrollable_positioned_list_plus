name: scrollable_positioned_list_plus
version: 0.4.0
description: >
  A customizable widget providing programmatic scrolling and index-based positioning capabilities with live telemetry for visible item boundaries.
repository: https://github.com/Ciudad-Valles-Open-Source/scrollable_positioned_list_plus
homepage: https://github.com/Ciudad-Valles-Open-Source/scrollable_positioned_list_plus
environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  collection: ^1.19.1

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
