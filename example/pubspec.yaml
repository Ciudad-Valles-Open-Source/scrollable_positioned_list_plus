name: scrollable_positioned_list_example

publish_to: "none"

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  scrollable_positioned_list_plus:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
